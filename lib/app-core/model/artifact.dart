class ArtifactModel {
  final int id;
  final String artifactType;
  final String artifactSetType;
  final String mainstat;
  final double mainStatValue;
  //substats

  ArtifactModel(this.id, this.artifactType, this.artifactSetType, this.mainstat,
      this.mainStatValue);
}
